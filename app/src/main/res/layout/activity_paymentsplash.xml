<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimary"
    tools:context=".activity.PaymentSplashActivity">

    <ImageView
        android:id="@+id/imgSplashPayment"
        android:layout_width="120dp"
        android:layout_height="110dp"
        android:layout_centerInParent="true"
        android:scaleType="fitXY"
        android:src="@drawable/tick" />
    
    <TextView
        android:id="@+id/txtSplashPayment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/your_order_has_been_successfully_placed"
        android:textColor="@color/white"
        android:layout_below="@id/imgSplashPayment"
        android:layout_marginLeft="5dp"
        android:layout_marginTop="10dp"
        android:layout_marginRight="5dp"
        android:gravity="center_horizontal"
        android:textSize="18sp"
        android:textStyle="bold"
        android:fontFamily="sans-serif-thin"/>
</RelativeLayout>